let turn = false;
let walls = {true: 10, false: 10}
let board = [];
let walls = new Set();
for (let i = 0; i < 9; i++){
	board.push([])
	for (let j = 0; j < 9; j++){
		board[i].push(0)
	}
}

function wallTo
function placeWall(id){
	if !walls[turn]:
		return;
	walls[turn]--;
	turn = !turn;
}
const walls= document.getElementsByClassName("hwall") + document.getElementsByClassName("vwall");
for (let i = 0; i < walls.length; i++){
	walls[i].addEventListener("click", function(){parseInt(walls[i].id)})
}
